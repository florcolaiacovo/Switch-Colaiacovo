import { StyleSheet, View } from 'react-native'
import React from 'react'

const CardDeLosCursos = ({children}) => {
  return (
    <View>
      {children}
    </View>
  )
}

export default CardDeLosCursos

const styles = StyleSheet.create({})